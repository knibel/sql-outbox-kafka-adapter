apiVersion: v1
kind: Service
metadata:
  name: {{ include "sql-outbox-kafka-adapter.fullname" . }}
  labels:
    {{- include "sql-outbox-kafka-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sql-outbox-kafka-adapter.selectorLabels" . | nindent 4 }}
